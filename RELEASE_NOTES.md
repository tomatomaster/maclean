# MacLean v1.0.0-beta.1

Mac用の重複ファイルクリーナーの初回ベータリリースです。

## 主な機能

### 重複ファイルスキャン

- フォルダを指定して再帰的にファイルを走査し、SHA-256ハッシュで完全一致する重複ファイルを検出します
- ファイルサイズによる事前グループ化で、不要なハッシュ計算をスキップし高速に動作します
- ディレクトリ走査・ハッシュ計算ともに並列処理（8ワーカー）で高速化しています

### フィルター機能

- **ファイルタイプフィルター**: 画像 / 音楽 / 動画 / ドキュメント / アーカイブから対象を絞り込み
- **最小サイズフィルター**: 1MB / 10MB / 100MB / 1GB 以上のファイルに絞って検索可能

### スキャン制御

- スキャン中いつでもキャンセル可能（ファイル収集中・ハッシュ計算中の両方に対応）
- キャンセル時はその時点までの結果を表示します

### 結果表示

- 重複グループを節約可能容量の大きい順にソート
- グループごとに展開/折りたたみ可能なコンパクトな一覧表示
- 各ファイルに更新日時を表示し、最新ファイルには「最新」タグを付与
- パスの共通部分を省略した見やすい相対パス表示
- 「全て展開」「全て折りたたむ」で一括操作

### ファイル削除

- 不要な重複ファイルを選択してゴミ箱に移動（安全に削除）
- 「最新を残す」ボタンでグループ内の最新ファイル以外を一括選択
- Finderでファイルの場所を直接表示可能

## 動作環境

- macOS（Intel / Apple Silicon 両対応のUniversalバイナリ）

## 既知の制限事項

- コード署名・公証（Notarization）は未対応のため、初回起動時にGatekeeperの警告が表示される場合があります。「システム設定 > プライバシーとセキュリティ」から許可してください
- 隠しファイル（`.`で始まるファイル）とシンボリックリンクはスキャン対象外です
- サイズが0バイトのファイルは重複検出の対象外です

## インストール方法

1. `MacLean-x.x.x-universal.dmg` をダウンロード
2. DMGを開き、MacLean.appをApplicationsフォルダにドラッグ
3. Applicationsフォルダから起動

### 初回起動時の注意

本アプリはAppleの公証（Notarization）を受けていないため、初回起動時に警告が表示されます。以下の手順で開けます：

1. MacLean.appを**右クリック（またはControlキーを押しながらクリック）**して「開く」を選択
2. 確認ダイアログで「開く」をクリック

> この操作は初回のみ必要です。2回目以降は通常通りダブルクリックで起動できます。
